# Contributing

- All server code lives in `src/`; keep new code there.
- Run the test suite with `npm test` before submitting changes.
- Run the server locally with `npm start` to verify behavior.
- Follow the existing ES module style and keep changes focused.
- Open a pull request against the base branch when ready.
