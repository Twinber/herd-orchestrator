{
  "name": "herdr-mcp",
  "version": "0.1.0",
  "description": "MCP server exposing the Herdr terminal API (JSON Schema) as callable tools",
  "type": "module",
  "bin": {
    "herdr-mcp": "server.js"
  },
  "scripts": {
    "start": "node server.js",
    "test": "node test/smoke.js",
    "typecheck": "node --check server.js && node --check src/schema.js && node --check src/client.js && node --check src/docs.js"
  },
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.25.0"
  }
}
